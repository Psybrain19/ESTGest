@model ESTGest.Models.User

<div>
    @using (Html.BeginForm("Login", "Account", new { returnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form", @id = "login-form" }))
            {
        @Html.AntiForgeryToken()
        <hr />
        @Html.ValidationSummary(true)
        <div class="form-group">
            <div class="col-md-10">
                @Html.TextBoxFor(m => m.u_number, new { @class = "form-control", @placeholder = "Number", @style= "margin-left: 16px;" })
                @Html.ValidationMessageFor(m => m.u_number)
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-10">
                @Html.PasswordFor(m => m.u_password, new { @class = "form-control", @placeholder = "Password", @style = "margin-left: 16px;" })
                @Html.ValidationMessageFor(m => m.u_password)
            </div>
        </div>
        @*<div class="form-group">
            <div class="col-md-offset-2 col-md-10" style="margin-top: -12px">
                <div class="checkbox">

            
                    <p>
                        @Html.ActionLink("Registar-me?", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" }), Caso não tenha conta.
                    </p>                 
                </div>
            </div>
        </div>*@
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10" style="margin-top: -12px">
                <input type="submit" value="Log in" class="btn btn-default" style="width: 170px; margin-top: 15px; margin-left: -22px;" />
            </div>
        </div>
    }
</div>

